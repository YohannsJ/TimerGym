<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema framework: la app no depende de Material Components XML
         (solo Compose), asi que el parent debe ser un tema de plataforma. -->
    <style name="Theme.TimerGym" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
